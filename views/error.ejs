<!DOCTYPE html>
<html>
<head>
    <title>Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="alert alert-danger">
            <h2>⚠️ <%= message %></h2>
            <% if (error.message) { %>
                <p class="mt-3"><strong>Details:</strong> <%= error.message %></p>
            <% } %>
            <a href="/plant" class="btn btn-primary mt-3">Return to Dashboard</a>
        </div>
    </div>
</body>
</html>